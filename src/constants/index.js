const BASE_URL = process.env.REACT_APP_BASE_URL;

const cinemaId = process.env.REACT_APP_OBJECT_ID;

const cityId = process.env.REACT_APP_CITY_ID;

const token = process.env.REACT_APP_AFISHA_TOKEN;

export { BASE_URL, cinemaId, cityId, token };
